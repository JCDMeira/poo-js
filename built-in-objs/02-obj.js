/*
  _ Object

  - pai de todos objs em js
  - possui props e métodos, mesmo o obj estando vazio
  - pode ser criado via new
*/

let o = new Object();

console.log(o.toString()); //? retorna a representação de um obj em string
console.log(o.valueOf()); //? retorna o próprio obj
